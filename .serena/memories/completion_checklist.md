## 任务结束前须知
- 至少运行 `python3 main.py`，验证循环赛输出与示例日志，确认角色改动或平衡调整符合预期。
- 如修改了类型或模块结构，执行 `uv run pyright` 确保没有导入/类型错误。
- 若新增测试脚本，需提供执行命令（推荐 `uv run python -m pytest`）并在说明中附上结果。
- 变更说明中要提及受影响的角色/模块、冷却或状态逻辑的行为差异，并提供任何胜率/日志对比数据。
- 遵循仓库指南：只提交相关文件，保持中文日志/注释；确认未遗留调试代码或多余输出。